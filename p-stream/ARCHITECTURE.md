# MNFLIX Integration Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         User's Browser                          │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │              P-Stream Frontend (React)                    │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────┐    │ │
│  │  │           Navigation Component                   │    │ │
│  │  │  [Home] [Discover] [🎬 MNFLIX] [Notifications]  │    │ │
│  │  └──────────────────────────────────────────────────┘    │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────┐    │ │
│  │  │            MNFLIX Pages                          │    │ │
│  │  │  ┌──────────────────────────────────────────┐   │    │ │
│  │  │  │ BrowsePage (/mnflix/browse)             │   │    │ │
│  │  │  │ - Fetches all movies from backend       │   │    │ │
│  │  │  │ - Displays grid of movie posters        │   │    │ │
│  │  │  └──────────────────────────────────────────┘   │    │ │
│  │  │  ┌──────────────────────────────────────────┐   │    │ │
│  │  │  │ MovieDetailPage (/mnflix/movie/:id)     │   │    │ │
│  │  │  │ - Fetches movie details                 │   │    │ │
│  │  │  │ - Shows poster, info, "Play" button     │   │    │ │
│  │  │  └──────────────────────────────────────────┘   │    │ │
│  │  │  ┌──────────────────────────────────────────┐   │    │ │
│  │  │  │ MNFLIXPlayerPage (/mnflix/player/:id)   │   │    │ │
│  │  │  │ - HLS.js video player                   │   │    │ │
│  │  │  │ - Progress tracking                     │   │    │ │
│  │  │  │ - Subtitle support                      │   │    │ │
│  │  │  └──────────────────────────────────────────┘   │    │ │
│  │  └──────────────────────────────────────────────────┘    │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────┐    │ │
│  │  │            Services Layer                        │    │ │
│  │  │  ┌────────────┐  ┌────────────┐  ┌────────────┐ │    │ │
│  │  │  │  api.ts    │  │ movies.ts  │  │ zenflify.ts│ │    │ │
│  │  │  │            │  │            │  │            │ │    │ │
│  │  │  │ - ofetch   │  │ - getAllM  │  │ - getStream│ │    │ │
│  │  │  │ - JWT auth │  │ - getById  │  │ - progress │ │    │ │
│  │  │  │ - headers  │  │ - trending │  │ - subtitles│ │    │ │
│  │  │  └────────────┘  └────────────┘  └────────────┘ │    │ │
│  │  │  ┌────────────┐                                  │    │ │
│  │  │  │  auth.ts   │                                  │    │ │
│  │  │  │            │                                  │    │ │
│  │  │  │ - login    │                                  │    │ │
│  │  │  │ - register │                                  │    │ │
│  │  │  │ - logout   │                                  │    │ │
│  │  │  └────────────┘                                  │    │ │
│  │  └──────────────────────────────────────────────────┘    │ │
│  │                          ↕ HTTP/REST                      │ │
│  │                 (Authorization: Bearer {JWT})             │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                               ↕
                        Internet/Network
                               ↕
┌─────────────────────────────────────────────────────────────────┐
│                      MNFLIX Backend Server                      │
│                    (http://localhost:4000)                      │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                    Express.js API                         │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────┐    │ │
│  │  │              API Endpoints                       │    │ │
│  │  │                                                   │    │ │
│  │  │  Movies:                                         │    │ │
│  │  │  GET  /api/movies                                │    │ │
│  │  │  GET  /api/movies/:id                            │    │ │
│  │  │  GET  /api/movies/trending                       │    │ │
│  │  │  GET  /api/movies/popular                        │    │ │
│  │  │                                                   │    │ │
│  │  │  Streaming (Zenflify):                           │    │ │
│  │  │  GET  /api/streams/:movieId                      │    │ │
│  │  │  GET  /api/subtitles/:movieId                    │    │ │
│  │  │                                                   │    │ │
│  │  │  Progress:                                       │    │ │
│  │  │  GET  /api/progress/:movieId                     │    │ │
│  │  │  POST /api/progress/:movieId                     │    │ │
│  │  │                                                   │    │ │
│  │  │  Authentication:                                 │    │ │
│  │  │  POST /api/auth/login                            │    │ │
│  │  │  POST /api/auth/register                         │    │ │
│  │  │  GET  /api/auth/me                               │    │ │
│  │  │                                                   │    │ │
│  │  │  Favorites:                                      │    │ │
│  │  │  POST /api/favorites                             │    │ │
│  │  │  GET  /api/favorites                             │    │ │
│  │  └──────────────────────────────────────────────────┘    │ │
│  │                          ↕                                 │ │
│  │  ┌──────────────────────────────────────────────────┐    │ │
│  │  │              MongoDB Database                    │    │ │
│  │  │  - movies collection                             │    │ │
│  │  │  - users collection                              │    │ │
│  │  │  - progress collection                           │    │ │
│  │  │  - favorites collection                          │    │ │
│  │  └──────────────────────────────────────────────────┘    │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                               ↕
┌─────────────────────────────────────────────────────────────────┐
│                    Zenflify / CDN Servers                       │
│  - HLS video streams (.m3u8)                                   │
│  - MP4 video files                                             │
│  - Subtitle files (.vtt)                                       │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagrams

### 1. Browse Movies Flow

```
User
  │
  │ 1. Click Film icon
  ↓
Navigation
  │
  │ 2. Navigate to /mnflix/browse
  ↓
BrowsePage
  │
  │ 3. Call getAllMovies()
  ↓
movies.ts
  │
  │ 4. GET /api/movies
  ↓
api.ts (add JWT header)
  │
  │ 5. HTTP Request
  ↓
MNFLIX Backend
  │
  │ 6. Query MongoDB
  ↓
MongoDB
  │
  │ 7. Return movies array
  ↓
MNFLIX Backend
  │
  │ 8. JSON Response
  ↓
api.ts
  │
  │ 9. Parse response
  ↓
movies.ts
  │
  │ 10. Return Movie[]
  ↓
BrowsePage
  │
  │ 11. Render movie grid
  ↓
User sees movies
```

### 2. Play Video Flow

```
User
  │
  │ 1. Click "Play" button
  ↓
MovieDetailPage
  │
  │ 2. Navigate to /mnflix/player/:id
  ↓
MNFLIXPlayerPage
  │
  ├─→ 3a. Call getMovieById(id)
  │      ↓
  │   movies.ts → Backend → Movie details
  │
  ├─→ 3b. Call getStreamingSourcesForMovie(id)
  │      ↓
  │   zenflify.ts → Backend → Stream URLs
  │
  └─→ 3c. Call getWatchProgress(id)
         ↓
      zenflify.ts → Backend → Last position
  │
  │ 4. Initialize HLS.js
  ↓
HLS.js
  │
  │ 5. Load .m3u8 playlist
  ↓
Zenflify/CDN
  │
  │ 6. Download video segments
  ↓
HLS.js
  │
  │ 7. Decode & play video
  ↓
<video> element
  │
  │ 8. User watches
  ↓
Every 10 seconds:
  │
  │ 9. Call saveWatchProgress()
  ↓
zenflify.ts
  │
  │ 10. POST /api/progress/:id
  ↓
MNFLIX Backend
  │
  │ 11. Update MongoDB
  ↓
Progress saved
```

### 3. Authentication Flow

```
User
  │
  │ 1. Enter email/password
  ↓
LoginPage
  │
  │ 2. Call login(email, password)
  ↓
auth.ts
  │
  │ 3. POST /api/auth/login
  ↓
api.ts (no auth header yet)
  │
  │ 4. HTTP Request
  ↓
MNFLIX Backend
  │
  │ 5. Verify credentials
  ↓
MongoDB
  │
  │ 6. Generate JWT token
  ↓
MNFLIX Backend
  │
  │ 7. Return {token, user}
  ↓
api.ts
  │
  │ 8. Parse response
  ↓
auth.ts
  │
  │ 9. Store in localStorage
  │     key: "__MW::auth"
  │     value: {state: {account: {token: "jwt..."}}}
  ↓
localStorage
  │
  │ 10. Redirect to home
  ↓
All future requests:
  │
  │ 11. api.ts reads token from localStorage
  │ 12. Adds "Authorization: Bearer {token}" header
  ↓
Backend receives authenticated requests
```

## Component Interaction Map

```
┌─────────────────────────────────────────────────────────┐
│                      App.tsx                            │
│                   (Route Manager)                       │
│  ┌───────────────────────────────────────────────────┐ │
│  │ /mnflix/browse      → BrowsePage                 │ │
│  │ /mnflix/movie/:id   → MovieDetailPage            │ │
│  │ /mnflix/player/:id  → MNFLIXPlayerPage           │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        ↓                ↓                ↓
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  BrowsePage  │  │ MovieDetail  │  │ MNFLIXPlayer │
│              │  │    Page      │  │    Page      │
│ - Grid view  │  │              │  │              │
│ - Click →    │──→ Detail view  │  │ - HLS.js     │
│   navigate   │  │              │  │ - Progress   │
│              │  │ - Play btn → │──→ - Subtitles  │
└──────────────┘  └──────────────┘  └──────────────┘
        │                │                │
        └────────────────┼────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│                   Services Layer                        │
│  ┌────────────────────────────────────────────────────┐ │
│  │  api.ts    movies.ts    zenflify.ts    auth.ts    │ │
│  └────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
                         ↓
                  MNFLIX Backend
```

## Technology Stack Layers

```
┌──────────────────────────────────────────────────────┐
│                   User Interface                     │
│  React Components, React Router, Tailwind CSS       │
└──────────────────────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────┐
│                  State Management                    │
│  useState, useEffect, React Context, Zustand         │
└──────────────────────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────┐
│                 Business Logic                       │
│  Custom hooks, Utility functions                     │
└──────────────────────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────┐
│                   API Layer                          │
│  ofetch (HTTP client), JWT auth, Error handling     │
└──────────────────────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────┐
│                Video Streaming                       │
│  HLS.js, Native HLS, MP4 fallback                   │
└──────────────────────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────┐
│                   Build Tools                        │
│  Vite, TypeScript, ESLint, PostCSS                  │
└──────────────────────────────────────────────────────┘
```

## File Dependencies

```
App.tsx
  ├── imports BrowsePage
  ├── imports MovieDetailPage
  └── imports MNFLIXPlayerPage

BrowsePage.tsx
  ├── imports HomeLayout
  ├── imports WideContainer
  └── imports getAllMovies from movies.ts

MovieDetailPage.tsx
  ├── imports HomeLayout
  ├── imports Button
  ├── imports getMovieById from movies.ts
  └── imports addToFavorites from movies.ts

MNFLIXPlayerPage.tsx
  ├── imports Hls from hls.js
  ├── imports getMovieById from movies.ts
  ├── imports getStreamingSourcesForMovie from zenflify.ts
  ├── imports getWatchProgress from zenflify.ts
  └── imports saveWatchProgress from zenflify.ts

movies.ts
  ├── imports api from api.ts
  └── imports Movie type from types/movie.ts

zenflify.ts
  ├── imports api from api.ts
  └── imports StreamingData, WatchProgress from types/movie.ts

auth.ts
  ├── imports api from api.ts
  └── imports AuthResponse, User from types/movie.ts

api.ts
  ├── imports ofetch
  └── imports conf from setup/config.ts

config.ts
  ├── reads import.meta.env.VITE_API_URL
  └── provides getApiUrl()
```

This architecture provides a clean separation of concerns, type safety, and maintainability.
